# Private runtime storage

This directory is reserved for private runtime data. Content under its operational subdirectories is ignored by Git; only `.gitkeep` placeholders are tracked. Web-server configuration must keep this directory outside the public document root where possible.
