# Database baseline

`db.sql` is a byte-for-byte verified copy of the legacy Pharmacy schema retained at the repository root for Phase 1 compatibility. The original root `db.sql` remains unchanged. Future schema changes must be additive, reviewed migrations in `migrations/`.
