# Unified authentication foundation

This directory reserves the public authentication boundary for the future shared platform. The current `login.php`, `register.php`, and `actions/` authentication flow remain untouched during Phase 1 to preserve Pharmacy POS compatibility.
